<% @title = _("{{site_name}} blog and tweets", :site_name => site_name) %>

<div id="left_column" class="left_column">
  <h1><%=@title %></h1>

  <div id="blog" class="blog">
    <% @blog_items.each do |item| %>
      <div class="blog_post">
        <h2 id="<%= Time.parse(item['pubDate']).to_i %>"><a href="<%=item['link']%>"><%=h item['title'] %></a></h2>
        <p class="subtitle"><%= _("Posted on {{date}} by {{author}}", :date=>simple_date(Time.parse(item['pubDate'])), :author=> item['creator'] ? item['creator'] : item['author']) %></p>
        <div>
          <% if item['encoded'] %>
            <%= sanitize(raw item['encoded']) %>
          <% elsif item['description'] %>
            <%= sanitize(raw item['description']) %>
          <% end %>
        </div>
        <p>
          <em>
            <% if item['comments'] %>
              <a href="<%=item['comments']%>"><%= _("{{number_of_comments}} comments", :number_of_comments=>item['comments']) %></a>
            <% end %>
          </em>
        </p>
      </div>
    <% end %>
  </div>
</div>

<% if !@twitter_user.empty? %>
  <div id="right_column" class="right_column" role="complementary">
    <div class="act_link">
      <h2><%= _("Stay up to date") %></h2>
      <%= image_tag "twitter.png", :alt => "twitter icon", :class => "twitter-icon" %> <a href="https://twitter.com/<%= @twitter_user %>"><%= _("Follow us on twitter") %></a><br/><br/>
      <%= image_tag "feed-16.png", :alt => "RSS icon" %> <a href="<%= @feed_url %>"><%= _("Subscribe to blog") %></a>
    </div>
    <% if AlaveteliConfiguration::twitter_widget_id %>
      <div id="twitter" class="twitter">
        <a class="twitter-timeline" data-dnt=true href="https://twitter.com/<%= AlaveteliConfiguration::twitter_username %>" data-widget-id="<%= AlaveteliConfiguration::twitter_widget_id %>">Tweets by @<%= AlaveteliConfiguration::twitter_username %></a>
        <script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0];if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src="//platform.twitter.com/widgets.js";fjs.parentNode.insertBefore(js,fjs);}}(document,"script","twitter-wjs");</script>
      </div>
    <% end %>
  </div>
<% end %>